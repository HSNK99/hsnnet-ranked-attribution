# Data Availability

BOSSBase 1.01 and BOWS2 images are not redistributed in this repository.
The repository provides pair identifiers, split evidence, detector checkpoints,
calibration artifacts, response vectors, per-image predictions, and table-
regeneration outputs. Dataset paths must be supplied locally through
`configs/paths.example.json`.
